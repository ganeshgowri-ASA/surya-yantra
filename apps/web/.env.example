# PostgreSQL connection string (Railway / Neon / Supabase)
DATABASE_URL="postgresql://user:password@host:5432/surya_yantra?schema=public"

# AI providers — at least one required for /diagnostics
ANTHROPIC_API_KEY="sk-ant-api03-..."
OPENAI_API_KEY="sk-proj-..."

# Public app URL (used for absolute links in emails/reports)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: default organization / test bed seed IDs
DEFAULT_ORG_ID=""
DEFAULT_TESTBED_ID=""
